---
description: auto-push all changes to github after every edit
---

// turbo-all

After making any code changes to files in this project, always run the following command to stage, commit, and push all modified files to GitHub:

1. Run this command from the project root (`c:\Users\mahir\Desktop\Desktop Folders\LEGO At Play\Remote Play Files\lego-isometric`):

```
git add -A; git commit -m "<short description of changes>"; git push
```

Replace `<short description of changes>` with a brief summary of what was changed (e.g. "Fix highlight button", "Add name input to lobby").

This should happen at the end of every task or set of code edits — no need to ask the user first.
